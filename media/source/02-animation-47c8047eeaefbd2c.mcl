import std.math
import std.mesh
import std.anim
import std.color

background = [0.035, 0.045, 0.060, 1]
let tile = |width| fill{alpha{0.12} CYAN} stroke{CYAN} Square(width)
mesh square = tile(1)

slide "square"
play Set()

square = rotate{PI / 4} tile(2)
play Trans(1)
