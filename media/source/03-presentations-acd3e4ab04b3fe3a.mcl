import std.math
import std.mesh
import std.anim
import std.color

background = [0.035, 0.045, 0.060, 1]
param radius = 1
mesh circle = fill{alpha{0.14} CYAN} stroke{CYAN} Circle(radius: $radius)

slide "radius"
play Set()
