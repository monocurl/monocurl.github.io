import std.math
import std.mesh
import std.anim
import std.color

background = [0.035, 0.045, 0.060, 1]
let wave = |x| sin(x * TAU)
mesh curve = stroke{CYAN} ExplicitFunc(wave)

slide "wave"
play Set()
