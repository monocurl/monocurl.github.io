import std.math
import std.mesh
import std.anim
import std.color

background = [0.035, 0.045, 0.060, 1]
mesh triangle = fill{alpha{0.12} CYAN} stroke{CYAN} Triangle(LEFT, RIGHT, UP)

slide "triangle"
play Write(1)

triangle = fill{alpha{0.10} GREEN} stroke{GREEN} Circle(1)
play Trans(1.2)
