import std.math
import std.mesh
import std.anim
import std.color

background = [0.035, 0.045, 0.060, 1]
mesh dot = fill{YELLOW} Dot(LEFT * 2)

slide "move"
play Set()

dot = fill{YELLOW} Dot(RIGHT * 2)
play Trans(1.2)
